# QRTC Examples

`telemetry-policy.json` declares the versioned product contract.

`telemetry-input.json` is a sanitized predecessor record suitable for product operation and replay.

`telemetry-model.json` is a minimal adequacy-analysis model used by the CLI demo command.